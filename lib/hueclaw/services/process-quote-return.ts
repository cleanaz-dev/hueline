export async function processQuoteReturn(task: any, result: any) {
  const quote = "quote";
  const threadId = "123345"


  return { releaseLock: true, threadId, message: "Imagen delivery complete" };
}
